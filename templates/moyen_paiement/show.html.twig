{% extends 'base.html.twig' %}

{% block title %}MoyenPaiement{% endblock %}

{% block body %}
    <h1>MoyenPaiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moyen_paiement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ moyen_paiement.nom }}</td>
            </tr>
            <tr>
                <th>NumeroMasque</th>
                <td>{{ moyen_paiement.numeroMasque }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ moyen_paiement.type.value }}</td>
            </tr>
            <tr>
                <th>LibelleBanque</th>
                <td>{{ moyen_paiement.libelleBanque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_moyen_paiement_index') }}">back to list</a>

    <a href="{{ path('app_moyen_paiement_edit', {'id': moyen_paiement.id}) }}">edit</a>

    {{ include('moyen_paiement/_delete_form.html.twig') }}
{% endblock %}
